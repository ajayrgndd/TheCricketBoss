name: Simulate Friendly Matches Cron

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes UTC
  workflow_dispatch:      # Also allow manual run

jobs:
  simulate-matches:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger simulate-due-matches
        run: |
          curl -X POST https://iukofcmatlfhfwcechdq.supabase.co/functions/v1/simulate-due-matches \
          -H "Authorization: Bearer ajay1243"
