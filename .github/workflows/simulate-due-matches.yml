name: 🔁 Simulate Due Matches (Every 5 Min)

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minutes
  workflow_dispatch:       # Allow manual trigger from GitHub UI

jobs:
  simulate-due-matches:
    runs-on: ubuntu-latest

    steps:
      - name: 🔐 Trigger Supabase Edge Function
        run: |
          curl -X POST "https://iukofcmatlfhfwcechdq.supabase.co/functions/v1/simulate-due-matches" \
          -H "Authorization: Bearer ${{ secrets.SIMULATE_KEY }}" \
          -H "Content-Type: application/json"
